// Form Prefiller Plugin - translations

gravFormPrefillerPlugin = (function () {

  function getTranslations() {
    const translations = {{ lang|raw }};

    return translations;
  }

  return {
    getTranslations: getTranslations,
  };
})();